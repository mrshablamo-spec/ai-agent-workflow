# Mini-Palantir Supply Chain Intelligence Engine
# All libraries are free and open source. No API keys required.

# SEC EDGAR data access
edgartools>=2.0.0

# HTML/XML parsing for 10-K filings and news scraping
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Data manipulation and analysis
pandas>=2.0.0

# Supply chain graph construction and analysis
networkx>=3.1

# Stock/financial data (free Yahoo Finance access)
yfinance>=0.2.30

# HTTP requests (used by scrapers with proper User-Agent headers)
requests>=2.31.0

# Graph visualization
matplotlib>=3.7.0

# Optional: lightweight NLP for entity extraction (use small model only)
# spacy>=3.6.0
# Run after install: python -m spacy download en_core_web_sm

# Testing
pytest>=7.4.0
